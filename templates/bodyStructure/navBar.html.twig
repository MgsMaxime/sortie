<nav>
    <ul>
        {% if is_granted("ROLE_USER") %}
            <a href="{{ path('sortie_accueil') }}">
                <li>Accueil</li>
            </a>
            {# TODO : modifier id #}
            <a href="{{ path('participant_modifier', {'id': app.user.id}) }}">
                <li>Mon profil</li>
            </a>
            <a href="{{ path('app_logout') }}">
                <li>se déconnecter</li>
            </a>
            {% if is_granted("ROLE_ADMIN") %}
                <a href="">
                    <li>Villes</li>
                </a>
                <a href="">
                    <li>Campus</li>
                </a>
            {% endif %}
        {% endif %}
    </ul>
</nav>
