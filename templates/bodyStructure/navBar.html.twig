<nav>
    <ul>
        {% if is_granted("ROLE_USER") %}
            <li>
                <a href="{{ path('main_accueil') }}">Accueil</a>
            </li>
            <li>
                <a href="{{ path('participant_modifier', {'id': app.user.id}) }}">Mon profil</a>
            </li>
            <li>
                <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </li>

            {% if is_granted("ROLE_ADMIN") %}
                <li>
                    <a href="{{ path('ville_afficher') }}">Villes</a>
                </li>
                <li>
                    <a href="">Campus</a>
                </li>
            {% endif %}
        {% endif %}
    </ul>
</nav>
